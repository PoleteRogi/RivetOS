from lib.style import *
import sys
import pygame.camera
import pygame.image
import sys


def init(m):
    pass

def camera(m):
    # APP STYLE SETTINGS
    set_background(BLACK)
    set_foreground(WHITE)

    set_primary(RED)

    initApp(m)